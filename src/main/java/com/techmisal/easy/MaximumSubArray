/*
Given an array of Integers we have to print an the maximum sum of a contigous sub array
Here we have an array = {-2,-3,4,-1,-2,1,5,-3}
The max-sum-contigous-array will be for (4+ -1 + -2 + 1 + 5)

Here we have taken two varaibles ,max_so_far and max_end_here.
max_so_far will keep track of our sum which will eventually become our answer.
max_end_here updates its sum after adding every new integer and updates itself and check against ma so far.


*/



public class Main{
	public static void main(String args[]){
		int max_so_far=0,max_end_here=0;
   		 int[] a={-2,-3,4,-1,-2,1,5,-3};
   		 for(int i=0;i<a.length;++i){
       			 max_end_here += a[i];
       			 if(max_end_here < 0){
            			max_end_here = 0;
       			 }else if(max_so_far < max_end_here){
           			 max_so_far = max_end_here;
       			 }
   		 }
   		 System.out.println(max_so_far);
   		 
	}
}
